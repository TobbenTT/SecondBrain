# SecondBrain Dashboard — Environment Variables
# Copy this file as .env and fill in your values

# Google Gemini API Key (get from https://aistudio.google.com/apikey)
GEMINI_API_KEY=your-gemini-api-key-here

# Session secret (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=change-me-to-a-random-string

# Server
PORT=3000
NODE_ENV=development

# 2FA encryption key (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# Required if 2FA (TOTP/WebAuthn) is enabled
TWOFA_ENCRYPTION_KEY=

# Internal API key for inteligencia-correos service (auto-generated if not set)
CORREOS_API_KEY=

# CORS — Allowed origins in production (comma-separated)
# Example: ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com
ALLOWED_ORIGINS=
