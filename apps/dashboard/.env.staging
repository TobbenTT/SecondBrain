# SecondBrain Dashboard — Staging Environment
# Los valores de PG se sobreescriben por docker-compose.staging.yml

# Google Gemini API Key (puede ser la misma de produccion)
GEMINI_API_KEY=your-gemini-api-key-here

# Session secret (DEBE ser diferente al de produccion)
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=staging-change-me-to-a-random-string

# Server
PORT=3010
NODE_ENV=staging

# PostgreSQL (local en staging)
PG_HOST=postgres-staging
PG_PORT=5432
PG_USER=secondbrain
PG_PASSWORD=staging_sb_2026
PG_DATABASE=secondbrain

# CORS
ALLOWED_ORIGINS=https://staging.aiprowork.com
