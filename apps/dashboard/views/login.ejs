<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar Sesión | PMO Corporate</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body,
        html {
            height: 100%;
            font-family: 'Inter', sans-serif;
            overflow: hidden;
        }

        .container {
            display: flex;
            height: 100vh;
            width: 100%;
        }

        /* LEFT SIDE - BRANDING */
        .left-panel {
            flex: 1.2;
            /* Slightly wider */
            background-color: #0052cc;
            /* Corporate Blue */
            color: #fff;
            position: relative;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 80px;
            overflow: hidden;
        }

        /* Decorative Circles */
        .left-panel::before {
            content: '';
            position: absolute;
            top: -100px;
            right: -100px;
            width: 400px;
            height: 400px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 50%;
        }

        .left-panel::after {
            content: '';
            position: absolute;
            bottom: -50px;
            left: -50px;
            width: 300px;
            height: 300px;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 50%;
        }

        .brand-header {
            position: absolute;
            top: 40px;
            left: 60px;
            font-size: 1.1rem;
            font-weight: 600;
            letter-spacing: -0.5px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .brand-header .vsc-badge {
            font-size: 0.8rem;
            font-weight: 800;
            opacity: 0.8;
        }

        .brand-content {
            z-index: 2;
        }

        .brand-title {
            font-size: 4rem;
            font-weight: 700;
            line-height: 1.1;
            margin-bottom: 20px;
        }

        .brand-subtitle {
            font-size: 1.1rem;
            opacity: 0.8;
            font-weight: 400;
            max-width: 400px;
            line-height: 1.5;
        }

        .brand-footer {
            position: absolute;
            bottom: 40px;
            left: 60px;
            font-size: 0.75rem;
            letter-spacing: 1px;
            opacity: 0.6;
            text-transform: uppercase;
            font-weight: 600;
        }

        /* RIGHT SIDE - FORM */
        .right-panel {
            flex: 1;
            background: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
        }

        .login-box {
            width: 100%;
            max-width: 400px;
            padding: 40px;
        }

        .login-header {
            margin-bottom: 40px;
        }

        .login-header h2 {
            font-size: 2rem;
            font-weight: 700;
            color: #1a202c;
            margin-bottom: 8px;
        }

        .form-group {
            margin-bottom: 24px;
        }

        .form-label {
            display: block;
            font-size: 0.7rem;
            font-weight: 700;
            color: #64748b;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
            font-family: 'Inter', sans-serif;
        }

        .form-input {
            width: 100%;
            padding: 16px 20px;
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            font-size: 0.95rem;
            color: #334155;
            outline: none;
            transition: all 0.2s;
            font-family: inherit;
        }

        .form-input:focus {
            background: #fff;
            border-color: #0052cc;
            box-shadow: 0 0 0 3px rgba(0, 82, 204, 0.1);
        }

        .form-input::placeholder {
            color: #94a3b8;
        }

        .btn-submit {
            width: 100%;
            padding: 16px;
            background-color: #0047BA;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s;
            margin-top: 10px;
        }

        .btn-submit:hover {
            background-color: #003a9a;
        }

        .form-footer {
            margin-top: 30px;
            text-align: center;
            font-size: 0.85rem;
            color: #64748b;
        }

        .form-footer a {
            color: #0047BA;
            text-decoration: none;
            font-weight: 600;
        }

        .form-footer a:hover {
            text-decoration: underline;
        }

        .error-message {
            background: #fee2e2;
            color: #b91c1c;
            padding: 12px;
            border-radius: 6px;
            font-size: 0.9rem;
            margin-bottom: 20px;
            text-align: center;
        }

        /* Responsive */
        @media (max-width: 900px) {
            .container {
                flex-direction: column;
                height: auto;
                min-height: 100vh;
                overflow-y: auto;
            }

            .left-panel {
                padding: 40px;
                min-height: 250px;
                flex: none;
                align-items: center;
                text-align: center;
            }

            .brand-header {
                position: static;
                margin-bottom: 20px;
            }

            .brand-footer {
                display: none;
            }

            .brand-title {
                font-size: 2.5rem;
            }

            .right-panel {
                padding: 40px 20px;
                flex: 1;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <!-- LEFT PANEL -->
        <div class="left-panel">
            <div class="brand-header">
                ValueStrategy Consulting <span class="vsc-badge">vsc</span>
            </div>
            <div class="brand-content">
                <h1 class="brand-title">ValueStrategy<br>Hub</h1>
                <p class="brand-subtitle">Centro de Inteligencia y Gestión de Activos</p>
            </div>
            <div class="brand-footer">
                VALUESTRATEGY CONSULTING © 2025
            </div>
        </div>

        <!-- RIGHT PANEL -->
        <div class="right-panel">
            <div class="login-box">
                <div class="login-header">
                    <h2>Inicio de Sesión</h2>
                </div>

                <% if (error) { %>
                    <div class="error-message">
                        <%= error %>
                    </div>
                    <% } %>

                        <form action="/login" method="POST">
                            <div class="form-group">
                                <label class="form-label">USUARIO</label>
                                <input type="text" name="username" class="form-input"
                                    placeholder="david, gonzalo, jose..." required autofocus>
                            </div>

                            <div class="form-group">
                                <label class="form-label">CONTRASEÑA</label>
                                <input type="password" name="password" class="form-input" placeholder="••••••••"
                                    required>
                            </div>

                            <button type="submit" class="btn-submit">Ingresar al Sistema</button>
                        </form>

                        <div class="form-footer">
                            ¿Problemas para acceder? <a href="#">Contacte a Soporte IT</a>
                        </div>
            </div>
        </div>
    </div>
</body>

</html>