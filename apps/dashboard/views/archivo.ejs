<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= basename %> — Documento interno de Value Strategy Consulting">
    <title>
        <%= basename %> — VSC Hub
    </title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon"
        href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>⚡</text></svg>">
</head>

<body>

    <!-- ═══════════════ SIDEBAR ═══════════════ -->
    <aside class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <a href="/" class="logo">
                <div class="logo-icon">VSC</div>
                <div class="logo-text">
                    <span class="logo-title">ValueStrategy</span>
                    <span class="logo-sub">Consulting Hub</span>
                </div>
            </a>
        </div>
        <nav class="sidebar-nav">
            <div class="nav-section">
                <span class="nav-label">Principal</span>
                <a href="/#overview" class="nav-link" data-section="overview">
                    <span class="nav-icon">📊</span> Overview
                </a>
                <a href="/#projects" class="nav-link" data-section="projects">
                    <span class="nav-icon">🚀</span> Proyectos
                </a>
            </div>
            <div class="nav-section">
                <span class="nav-label">Recursos</span>
                <a href="/#archivos" class="nav-link active" data-section="archivos">
                    <span class="nav-icon">📄</span> Archivos
                </a>
                <a href="/#methodologies" class="nav-link" data-section="methodologies">
                    <span class="nav-icon">⚡</span> Metodologías
                </a>
            </div>
        </nav>
        <div class="sidebar-footer">
            <div class="sidebar-status">
                <span class="status-dot"></span>
                <span>Sistema Activo</span>
            </div>
        </div>
    </aside>

    <!-- ═══════════════ MOBILE TOPBAR ═══════════════ -->
    <div class="topbar" id="topbar">
        <button class="sidebar-toggle" id="sidebarToggle" aria-label="Menu">☰</button>
        <div class="topbar-logo">
            <div class="logo-icon sm">VSC</div>
            <span>ValueStrategy Hub</span>
        </div>
    </div>
    <div class="sidebar-overlay" id="sidebarOverlay"></div>

    <!-- ═══════════════ ARCHIVO DETAIL ═══════════════ -->
    <div class="archivo-detail">
        <div>
            <a href="/#archivos" class="archivo-back">← Volver a Archivos</a>

            <div class="archivo-detail-header">
                <div class="archivo-icon <%= type === 'markdown' ? 'md' : type === 'pdf' ? 'pdf' : 'md' %>">
                    <%= type==='markdown' ? '📝' : type==='pdf' ? '📕' : '📄' %>
                </div>
                <div class="archivo-detail-title">
                    <h1>
                        <%= basename %>
                    </h1>
                    <div class="archivo-detail-meta">
                        <span>
                            <%= type.toUpperCase() %>
                        </span>
                        <span>•</span>
                        <span>
                            <%= size %>
                        </span>
                        <span>•</span>
                        <span>
                            <%= new Date(modified).toLocaleDateString('es-ES', { year: 'numeric' , month: 'long' ,
                                day: 'numeric' }) %>
                        </span>
                    </div>
                </div>
            </div>

            <% if (type==='markdown' && content) { %>
                <div class="markdown-content"><%- content %></div>
                <% } else if (type==='pdf' ) { %>
                    <iframe class="pdf-viewer" src="/archivos-file/<%= encodeURIComponent(filename) %>"
                        title="<%= basename %>"></iframe>
                    <% } else { %>
                        <div class="archivos-empty">
                            <div class="empty-icon">📄</div>
                            <p>Este tipo de archivo no se puede previsualizar.</p>
                        </div>
                        <% } %>
        </div>
    </div>

    <script>
        // Mobile sidebar toggle
        const toggle = document.getElementById('sidebarToggle');
        const sidebar = document.getElementById('sidebar');
        const overlay = document.getElementById('sidebarOverlay');
        if (toggle) {
            toggle.addEventListener('click', () => {
                sidebar.classList.toggle('open');
                overlay.classList.toggle('show');
            });
        }
        if (overlay) {
            overlay.addEventListener('click', () => {
                sidebar.classList.remove('open');
                overlay.classList.remove('show');
            });
        }
    </script>
</body>

</html>