<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verificacion 2FA | PMO Corporate</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body,
        html {
            height: 100%;
            font-family: 'Inter', sans-serif;
            overflow: hidden;
        }

        .container {
            display: flex;
            height: 100vh;
            width: 100%;
        }

        .left-panel {
            flex: 1.2;
            background-color: #0052cc;
            color: #fff;
            position: relative;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 80px;
            overflow: hidden;
        }

        .left-panel::before {
            content: '';
            position: absolute;
            top: -100px;
            right: -100px;
            width: 400px;
            height: 400px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 50%;
        }

        .left-panel::after {
            content: '';
            position: absolute;
            bottom: -50px;
            left: -50px;
            width: 300px;
            height: 300px;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 50%;
        }

        .brand-header {
            position: absolute;
            top: 40px;
            left: 60px;
            font-size: 1.1rem;
            font-weight: 600;
            letter-spacing: -0.5px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .brand-header .vsc-badge {
            font-size: 0.8rem;
            font-weight: 800;
            opacity: 0.8;
        }

        .brand-content {
            z-index: 2;
        }

        .brand-title {
            font-size: 4rem;
            font-weight: 700;
            line-height: 1.1;
            margin-bottom: 20px;
        }

        .brand-subtitle {
            font-size: 1.1rem;
            opacity: 0.8;
            font-weight: 400;
            max-width: 400px;
            line-height: 1.5;
        }

        .brand-footer {
            position: absolute;
            bottom: 40px;
            left: 60px;
            font-size: 0.75rem;
            letter-spacing: 1px;
            opacity: 0.6;
            text-transform: uppercase;
            font-weight: 600;
        }

        .right-panel {
            flex: 1;
            background: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
        }

        .login-box {
            width: 100%;
            max-width: 400px;
            padding: 40px;
        }

        .login-header {
            margin-bottom: 32px;
        }

        .login-header h2 {
            font-size: 1.8rem;
            font-weight: 700;
            color: #1a202c;
            margin-bottom: 8px;
        }

        .login-header p {
            font-size: 0.9rem;
            color: #64748b;
            line-height: 1.5;
        }

        .shield-icon {
            font-size: 2.5rem;
            margin-bottom: 16px;
        }

        .form-group {
            margin-bottom: 24px;
        }

        .form-label {
            display: block;
            font-size: 0.7rem;
            font-weight: 700;
            color: #64748b;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
        }

        .form-input {
            width: 100%;
            padding: 16px 20px;
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            font-size: 0.95rem;
            color: #334155;
            outline: none;
            transition: all 0.2s;
            font-family: inherit;
        }

        .form-input:focus {
            background: #fff;
            border-color: #0052cc;
            box-shadow: 0 0 0 3px rgba(0, 82, 204, 0.1);
        }

        .totp-input {
            text-align: center;
            font-size: 2rem;
            font-weight: 700;
            letter-spacing: 12px;
            padding: 16px;
        }

        .btn-submit {
            width: 100%;
            padding: 16px;
            background-color: #0047BA;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s;
            margin-top: 10px;
        }

        .btn-submit:hover {
            background-color: #003a9a;
        }

        .btn-submit:disabled {
            background-color: #94a3b8;
            cursor: not-allowed;
        }

        .error-message {
            background: #fee2e2;
            color: #b91c1c;
            padding: 12px;
            border-radius: 6px;
            font-size: 0.9rem;
            margin-bottom: 20px;
            text-align: center;
        }

        .trust-option {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-top: 16px;
            font-size: 0.85rem;
            color: #64748b;
        }

        .trust-option input[type="checkbox"] {
            width: 18px;
            height: 18px;
            accent-color: #0047BA;
        }

        .form-footer {
            margin-top: 24px;
            text-align: center;
            font-size: 0.85rem;
            color: #64748b;
        }

        .form-footer a {
            color: #0047BA;
            text-decoration: none;
            font-weight: 600;
        }

        .form-footer a:hover {
            text-decoration: underline;
        }

        .backup-toggle {
            color: #0047BA;
            cursor: pointer;
            font-weight: 600;
            font-size: 0.85rem;
            background: none;
            border: none;
            margin-top: 12px;
        }

        .backup-toggle:hover {
            text-decoration: underline;
        }

        .backup-section {
            display: none;
            margin-top: 16px;
        }

        .backup-section.visible {
            display: block;
        }

        .risk-badge {
            display: inline-flex;
            align-items: center;
            gap: 4px;
            padding: 4px 10px;
            border-radius: 12px;
            font-size: 0.72rem;
            font-weight: 600;
            margin-top: 8px;
        }

        .risk-high { background: #fef2f2; color: #dc2626; }
        .risk-medium { background: #fffbeb; color: #d97706; }

        @media (max-width: 900px) {
            .container {
                flex-direction: column;
                height: auto;
                min-height: 100vh;
                overflow-y: auto;
            }

            .left-panel {
                padding: 40px;
                min-height: 200px;
                flex: none;
                align-items: center;
                text-align: center;
            }

            .brand-header {
                position: static;
                margin-bottom: 20px;
            }

            .brand-footer {
                display: none;
            }

            .brand-title {
                font-size: 2.5rem;
            }

            .right-panel {
                padding: 40px 20px;
                flex: 1;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="left-panel">
            <div class="brand-header">
                ValueStrategy Consulting <span class="vsc-badge">vsc</span>
            </div>
            <div class="brand-content">
                <h1 class="brand-title">ValueStrategy<br>Hub</h1>
                <p class="brand-subtitle">Centro de Inteligencia y Gestion de Activos</p>
            </div>
            <div class="brand-footer">
                VALUESTRATEGY CONSULTING &copy; 2025
            </div>
        </div>

        <div class="right-panel">
            <div class="login-box">
                <div class="login-header">
                    <div class="shield-icon">&#128274;</div>
                    <h2>Verificacion 2FA</h2>
                    <p>Ingresa el codigo de tu aplicacion autenticadora (Google Authenticator, Microsoft Authenticator, etc.)</p>
                    <% if (riskReasons && riskReasons.length > 0) { %>
                    <div class="risk-badge <%= riskScore >= 50 ? 'risk-high' : 'risk-medium' %>">
                        &#9888; <%= riskReasons.join(', ') %>
                    </div>
                    <% } %>
                </div>

                <% if (error) { %>
                <div class="error-message"><%= error %></div>
                <% } %>

                <form action="/login/verify-2fa" method="POST" id="totpForm">
                    <div class="form-group">
                        <label class="form-label">CODIGO DE VERIFICACION</label>
                        <input type="text" name="token" id="totpInput" class="form-input totp-input"
                            placeholder="000000" maxlength="6" pattern="[0-9]{6}"
                            inputmode="numeric" autocomplete="one-time-code" required autofocus>
                    </div>

                    <label class="trust-option">
                        <input type="checkbox" name="trustDevice" value="1">
                        Confiar en este dispositivo por 30 dias
                    </label>

                    <button type="submit" class="btn-submit" id="verifyBtn">Verificar</button>

                    <div style="text-align: center; margin-top: 16px;">
                        <button type="button" class="backup-toggle" onclick="toggleBackup()">
                            Usar codigo de respaldo
                        </button>
                    </div>

                    <div class="backup-section" id="backupSection">
                        <div class="form-group">
                            <label class="form-label">CODIGO DE RESPALDO</label>
                            <input type="text" name="backupCode" class="form-input"
                                placeholder="12345678" maxlength="8" pattern="[0-9]{8}" inputmode="numeric">
                        </div>
                    </div>
                </form>

                <div class="form-footer">
                    <a href="/login">Cancelar e ir al login</a>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Auto-submit when 6 digits entered
        const totpInput = document.getElementById('totpInput');
        const form = document.getElementById('totpForm');
        totpInput.addEventListener('input', function () {
            this.value = this.value.replace(/\D/g, '');
            if (this.value.length === 6) {
                form.submit();
            }
        });

        function toggleBackup() {
            const section = document.getElementById('backupSection');
            section.classList.toggle('visible');
            if (section.classList.contains('visible')) {
                section.querySelector('input').focus();
            }
        }
    </script>
</body>

</html>
