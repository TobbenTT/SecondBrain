# Inteligencia de Correos — Staging Environment
# Copia este archivo y llena los valores reales en el VPS

# --- BASE DE DATOS (Supabase o PostgreSQL) ---
# Puede apuntar a la misma Supabase (solo lectura en staging)
DB_HOST=your-staging-db-host
DB_PORT=5432
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=inteligencia_correos

# --- INTELIGENCIA ARTIFICIAL ---
AI_API_KEY=your-gemini-api-key-here
AI_MODEL=gemini-3-flash-preview

# --- CORREO (usar email de prueba, NO produccion) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=staging-test@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=staging-test@gmail.com

# --- SERVIDOR ---
PORT=3013
WEBHOOK_SECRET=staging-webhook-secret

# --- DASHBOARD (apunta al staging) ---
DASHBOARD_URL=http://staging-dashboard:3010
