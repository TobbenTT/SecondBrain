# Inteligencia de Correos — Staging Environment
# Los valores de DB se sobreescriben por docker-compose.staging.yml

# --- BASE DE DATOS (PostgreSQL local en staging) ---
DB_HOST=postgres-staging
DB_PORT=5432
DB_USER=secondbrain
DB_PASSWORD=staging_sb_2026
DB_NAME=secondbrain
DB_SSL=false

# --- INTELIGENCIA ARTIFICIAL ---
AI_API_KEY=your-gemini-api-key-here
AI_MODEL=gemini-3-flash-preview

# --- CORREO (usar email de prueba, NO produccion) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=staging-test@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=staging-test@gmail.com

# --- SERVIDOR ---
PORT=3013
WEBHOOK_SECRET=staging-webhook-secret

# --- DASHBOARD (apunta al staging) ---
DASHBOARD_URL=http://dashboard-staging:3010
