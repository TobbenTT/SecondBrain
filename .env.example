# ─── SecondBrain — Variables de Entorno ───────────────────────────────────────
# Copiar a .env y configurar valores reales.
# Cada servicio tiene su propio .env; este archivo documenta TODAS las variables.

# ─── Dashboard (apps/dashboard/) ─────────────────────────────────────────────
# Ollama es el motor primario. Gemini es fallback (opcional).
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1
OLLAMA_TIMEOUT=300000
GEMINI_API_KEY=your-gemini-api-key
SESSION_SECRET=change-me-to-a-random-string
PORT=3000
NODE_ENV=development
ALLOWED_ORIGINS=

# ─── Supabase Auth (autenticación de usuarios) ──────────────────────────────
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ─── OpenClaw (openclaw/) ────────────────────────────────────────────────────
# Ver openclaw/.env.example para la lista completa de variables.
# Ollama es primario. Cloud keys son opcionales (fallback).
ANTHROPIC_API_KEY=your-claude-key
GEMINI_MODEL=gemini-3-flash-preview
CLAUDE_MODEL=claude-sonnet-4-6
LOCAL_MODEL=llama3.1
OLLAMA_URL=http://localhost:11434/api/generate
DB_PATH=../apps/dashboard/data/second_brain.db
SKILLS_DIR=../core/skills

# ─── Opcional: WhatsApp (Twilio) ─────────────────────────────────────────────
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=
TU_NUMERO_WHATSAPP=
